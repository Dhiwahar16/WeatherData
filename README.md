This project is done in java maven ( IDE - intelliji idea community )

What it can do -> Read and display the Data from given input file and display it in the console 

Used only Java to do this ( no database connected )
